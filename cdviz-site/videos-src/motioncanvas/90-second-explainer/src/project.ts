import { makeProject } from "@motion-canvas/core";

import explainer from "./scenes/explainer?scene";

export default makeProject({
  scenes: [explainer],
  name: "CDviz 90-Second Explainer - Vertical",
});
