{
  "timezone": "browser",
  "editable": true,
  "graphTooltip": 0,
  "fiscalYearStartMonth": 0,
  "schemaVersion": 42,
  "templating": {
    "list": [
      {
        "type": "datasource",
        "name": "datasource",
        "skipUrlSync": false,
        "multi": false,
        "allowCustomValue": true,
        "includeAll": false,
        "auto": false,
        "auto_min": "10s",
        "auto_count": 30,
        "query": "grafana-postgresql-datasource",
        "regex": "cdviz.*",
        "hide": 3
      },
      {
        "type": "query",
        "name": "artifact_fnames",
        "skipUrlSync": false,
        "multi": true,
        "allowCustomValue": true,
        "includeAll": true,
        "auto": false,
        "auto_min": "10s",
        "auto_count": 30,
        "label": "Artifacts",
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "${datasource}"
        },
        "query": "SELECT DISTINCT\n  COALESCE(\n    SUBSTRING(node_id FROM 'pkg:([^@\\\\?]+)'),\n    SUBSTRING(node_id FROM 'pkg:(.+)')\n  ) || E'\\\n' ||\n  COALESCE((regexp_match(node_id, 'repository_url=([^&]+)'))[1], '') AS __value\nFROM cdviz.graph_nodes\nWHERE node_type = 'artifact'\n  AND node_id LIKE 'pkg:%'\n  AND (node_id LIKE 'pkg:%@%' OR node_id LIKE 'pkg:%?%')\nORDER BY 1",
        "sort": 5,
        "refresh": 2,
        "current": {
          "text": "All",
          "value": "$__all"
        },
        "description": "Artifact type + namespace + name + repository"
      }
    ]
  },
  "annotations": {},
  "title": "Artifact: Timeline",
  "uid": "artifact_timeline",
  "tags": [
    "cd",
    "artifact",
    "service"
  ],
  "time": {
    "from": "now-31d",
    "to": "now"
  },
  "panels": [
    {
      "type": "volkovlabs-echarts-panel",
      "transparent": false,
      "repeatDirection": "h",
      "options": {
        "getOption": "// src/panels/browser_scripts/artifact_info.ts\nclass ArtifactInfo {\n  base;\n  repositoryUrl;\n  version;\n  tags;\n  constructor(artifactId) {\n    const url = new URL(artifactId);\n    const [base, version] = url.pathname.split(\"@\");\n    this.base = base;\n    this.version = version ? decodeURIComponent(version) : version;\n    this.tags = new Set(url.searchParams.getAll(\"tag\"));\n    this.repositoryUrl = url.searchParams.get(\"repository_url\");\n  }\n  isSimilarTo(other, _debug) {\n    let res = this.base === other.base && this.repositoryUrl === other.repositoryUrl;\n    if (!!this.version && !!other.version) {\n      res = res && this.version === other.version;\n    } else if (this.tags.size > 0 && other.tags.size > 0) {\n      const intersection = this.tags.intersection(other.tags);\n      intersection.delete(\"latest\");\n      res = res && intersection.size > 0;\n    } else {\n      const nonLatestSelf = new Set(this.tags);\n      nonLatestSelf.delete(\"latest\");\n      const nonLatestOther = new Set(other.tags);\n      nonLatestOther.delete(\"latest\");\n      res = res && this.version === other.version && nonLatestSelf.size === 0 && nonLatestOther.size === 0;\n    }\n    return res;\n  }\n  get hasVersion() {\n    const nonLatestTags = new Set(this.tags);\n    nonLatestTags.delete(\"latest\");\n    return !!this.version || nonLatestTags.size > 0;\n  }\n  mergeVersionAndTags(other) {\n    this.version = this.version || other.version;\n    this.tags = this.tags.union(other.tags);\n  }\n}\n\n// src/panels/browser_scripts/data_stages.ts\nvar REPO_SEPARATOR_PREFIX = \"\\x00repo:\";\nvar STAGE_KEY_SEP = \"\\x01\";\nfunction makeStageKey(stage, repo) {\n  return repo !== null ? `${repo}${STAGE_KEY_SEP}${stage}` : stage;\n}\nfunction parseStageKey(key) {\n  const sep = key.indexOf(STAGE_KEY_SEP);\n  if (sep === -1)\n    return { repo: null, stage: key };\n  return { repo: key.slice(0, sep), stage: key.slice(sep + 1) };\n}\nfunction isSeparator(key) {\n  return key.startsWith(REPO_SEPARATOR_PREFIX);\n}\nfunction separatorRepo(key) {\n  return key.slice(REPO_SEPARATOR_PREFIX.length);\n}\nfunction transformData(data) {\n  const series = groupBySeries(data);\n  const domains = extractDomains(series);\n  return {\n    series,\n    domains\n  };\n}\nfunction groupBySeries(data) {\n  const res = [];\n  for (const it of data) {\n    try {\n      const artifactInfo = new ArtifactInfo(it.artifact_id);\n      let dest = res.find((el) => artifactInfo.isSimilarTo(el[0]));\n      if (!dest) {\n        dest = [artifactInfo, []];\n        res.push(dest);\n      }\n      const nonLatestTags = [...artifactInfo.tags].filter((t) => t !== \"latest\").sort();\n      const lineKey = nonLatestTags.length > 0 ? nonLatestTags[0] : artifactInfo.version ?? \"\";\n      dest[0].mergeVersionAndTags(artifactInfo);\n      const datum = {\n        timestamp: it.timestamp,\n        action: it.action,\n        stage: it.stage,\n        artifactInfo: dest[0],\n        lineKey,\n        entity_type: it.entity_type,\n        entity_id: it.entity_id,\n        environment_id: it.environment_id\n      };\n      dest[1].push(datum);\n    } catch (e) {\n      console.error(\"Error parsing artifact_id:\", it, e);\n      throw e;\n    }\n  }\n  return res.map((el) => {\n    const sequence = el[1];\n    sequence.sort((a, b) => {\n      let r = a.timestamp - b.timestamp;\n      if (r === 0) {\n        r = a.stage.localeCompare(b.stage);\n      }\n      return r;\n    });\n    return sequence;\n  });\n}\nfunction extractDomains(data) {\n  let timestampMin = Number.MAX_SAFE_INTEGER;\n  let timestampMax = Number.MIN_SAFE_INTEGER;\n  let hasItem = false;\n  for (const serie of data) {\n    for (const d of serie) {\n      if (timestampMin > d.timestamp) {\n        timestampMin = d.timestamp;\n      }\n      if (timestampMax < d.timestamp) {\n        timestampMax = d.timestamp;\n      }\n      hasItem = true;\n    }\n  }\n  if (!hasItem) {\n    return {\n      timestampMin: Date.now(),\n      timestampMax: Date.now(),\n      stages: [],\n      multiRepo: false\n    };\n  }\n  const reposSeen = new Set;\n  for (const serie of data) {\n    for (const d of serie) {\n      reposSeen.add(d.artifactInfo.repositoryUrl);\n    }\n  }\n  const repos = [...reposSeen];\n  const multiRepo = repos.length > 1;\n  let stages;\n  if (!multiRepo) {\n    stages = sortStages(data);\n  } else {\n    stages = [];\n    for (const repo of repos) {\n      const repoSeries = data.map((serie) => serie.filter((d) => d.artifactInfo.repositoryUrl === repo)).filter((s) => s.length > 0);\n      const repoStages = sortStages(repoSeries);\n      stages.push(...repoStages.map((s) => makeStageKey(s, repo)));\n      stages.push(REPO_SEPARATOR_PREFIX + (repo ?? \"\"));\n    }\n  }\n  return {\n    timestampMin,\n    timestampMax,\n    stages,\n    multiRepo\n  };\n}\nfunction sortStages(seriesValues) {\n  const graph = buildTransitionGraph(seriesValues);\n  const hasIncoming = new Set;\n  for (const neighbors of graph.values()) {\n    for (const n of neighbors)\n      hasIncoming.add(n);\n  }\n  const isolated = new Set;\n  for (const [node, neighbors] of graph) {\n    if (neighbors.size === 0 && !hasIncoming.has(node))\n      isolated.add(node);\n  }\n  let stagesSorted = [];\n  try {\n    stagesSorted = topologicalSort(graph);\n  } catch (e) {\n    console.debug(\"Topological sort failed:\", graph);\n    console.error(\"Error during topological sort:\", e);\n    console.warn(\"fallback to alphabetical sort\");\n    stagesSorted = graph.keys().toArray().sort();\n  }\n  stagesSorted.reverse();\n  if (isolated.size > 0) {\n    const connected = stagesSorted.filter((s) => !isolated.has(s));\n    const isolatedArr = stagesSorted.filter((s) => isolated.has(s));\n    stagesSorted = [...connected, ...isolatedArr];\n  }\n  return stagesSorted;\n}\nfunction buildTransitionGraph(seriesValues) {\n  const graph = new Map;\n  for (const sequence of seriesValues) {\n    const stagesSeen = new Set;\n    for (let i = 0;i < sequence.length - 1; i++) {\n      const currentStage = sequence[i].stage;\n      const nextStage = sequence[i + 1].stage;\n      if (stagesSeen.has(nextStage)) {\n        continue;\n      }\n      stagesSeen.add(currentStage);\n      if (!graph.has(currentStage)) {\n        graph.set(currentStage, new Set);\n      }\n      if (currentStage !== nextStage) {\n        graph.get(currentStage)?.add(nextStage);\n      }\n      if (!graph.has(nextStage)) {\n        graph.set(nextStage, new Set);\n      }\n    }\n    if (sequence.length > 0) {\n      const lastStage = sequence[sequence.length - 1].stage;\n      if (!graph.has(lastStage)) {\n        graph.set(lastStage, new Set);\n      }\n    }\n  }\n  return graph;\n}\nfunction topologicalSort(graph) {\n  const inDegree = new Map;\n  const queue = [];\n  const topoOrder = [];\n  graph.forEach((neighbors, node) => {\n    inDegree.set(node, inDegree.get(node) || 0);\n    for (const neighbor of neighbors) {\n      inDegree.set(neighbor, (inDegree.get(neighbor) || 0) + 1);\n    }\n  });\n  inDegree.forEach((degree, node) => {\n    if (degree === 0) {\n      queue.push(node);\n    }\n  });\n  while (queue.length > 0) {\n    const node = queue.shift();\n    if (!node) {\n      continue;\n    }\n    topoOrder.push(node);\n    const neighbors = graph.get(node) || [];\n    for (const neighbor of neighbors) {\n      const updatedDegree = (inDegree.get(neighbor) || 0) - 1;\n      inDegree.set(neighbor, updatedDegree);\n      if (updatedDegree === 0) {\n        queue.push(neighbor);\n      }\n    }\n  }\n  if (topoOrder.length !== graph.size) {\n    throw new Error(\"Graph has at least one cycle, topological sort not possible.\");\n  }\n  return topoOrder;\n}\n\nclass StageSummary {\n  stage;\n  countTimestamp;\n  firstTimestamp;\n  lastTimestamp;\n  lastVersion;\n  countInterval;\n  totalInterval;\n  constructor(stage) {\n    this.stage = stage;\n    this.countTimestamp = 0;\n    this.firstTimestamp = null;\n    this.lastTimestamp = null;\n    this.lastVersion = null;\n    this.countInterval = 0;\n    this.totalInterval = 0;\n  }\n  registerEvent(timestamp, version, fromTimestamp) {\n    this.countTimestamp += 1;\n    if (this.firstTimestamp === null || timestamp < this.firstTimestamp) {\n      this.firstTimestamp = timestamp;\n    }\n    if (this.lastTimestamp === null || timestamp > this.lastTimestamp) {\n      this.lastTimestamp = timestamp;\n      this.lastVersion = version;\n    }\n    if (fromTimestamp !== null) {\n      this.countInterval += 1;\n      this.totalInterval += Math.abs(timestamp - fromTimestamp);\n    }\n  }\n  getIntervalAverage() {\n    return this.countInterval > 0 ? this.totalInterval / this.countInterval : 0;\n  }\n  getSummary() {\n    return {\n      stage: this.stage,\n      countTimestamp: this.countTimestamp,\n      firstTimestamp: this.firstTimestamp,\n      lastTimestamp: this.lastTimestamp,\n      lastVersion: this.lastVersion,\n      intervalAverage: this.getIntervalAverage()\n    };\n  }\n}\nfunction summarizeStages(seriesValues, multiRepo = false) {\n  const summaries = new Map;\n  for (const sequence of seriesValues) {\n    let previousTimestamp = null;\n    for (const datum of sequence) {\n      const key = multiRepo ? makeStageKey(datum.stage, datum.artifactInfo.repositoryUrl) : datum.stage;\n      const timestamp = datum.timestamp;\n      const version = datum.artifactInfo.version || datum.artifactInfo.tags.values().next().value || \"unknown\";\n      if (!summaries.has(key)) {\n        summaries.set(key, new StageSummary(key));\n      }\n      const summary = summaries.get(key);\n      if (summary) {\n        summary.registerEvent(timestamp, version, previousTimestamp);\n      }\n      previousTimestamp = timestamp;\n    }\n  }\n  return summaries;\n}\nfunction summarizeSortedStages(seriesValues, stages, multiRepo = false) {\n  const summaries = summarizeStages(seriesValues, multiRepo);\n  return stages.map((stage) => summaries.get(stage) || new StageSummary(stage));\n}\n\n// src/panels/browser_scripts/draw_timeline_echarts.ts\nfunction dataFramesToDatums(series) {\n  if (!series?.length)\n    return [];\n  const frame = series[0];\n  const getField = (name) => frame.fields.find((f) => f.name === name);\n  const timestampField = getField(\"timestamp\");\n  const actionField = getField(\"action\");\n  const stageField = getField(\"stage\");\n  const artifactIdField = getField(\"artifact_id\");\n  const entityTypeField = getField(\"entity_type\");\n  const entityIdField = getField(\"entity_id\");\n  const environmentIdField = getField(\"environment_id\");\n  if (!timestampField)\n    return [];\n  return timestampField.values.map((ts, i) => ({\n    timestamp: typeof ts === \"number\" ? ts : new Date(ts).getTime(),\n    action: actionField?.values[i] ?? \"\",\n    stage: stageField?.values[i] ?? \"\",\n    artifact_id: artifactIdField?.values[i] ?? \"\",\n    entity_type: entityTypeField?.values[i] || undefined,\n    entity_id: entityIdField?.values[i] || undefined,\n    environment_id: environmentIdField?.values[i] || undefined\n  }));\n}\nfunction stageDisplayLabel(stage) {\n  const parts = stage.split(`\n`);\n  if (parts.length === 1)\n    return stage;\n  if (parts.length === 2)\n    return `${parts[0]}\n${parts[1]}`;\n  return `${parts[0]}\n${parts[2]}`;\n}\nfunction durationFormat(duration) {\n  if (duration <= 0)\n    return \"\";\n  const seconds = Math.floor(duration / 1000);\n  if (seconds < 60)\n    return `${seconds}s`;\n  const minutes = Math.floor(seconds / 60);\n  if (minutes < 60)\n    return `${minutes}m ${seconds % 60}s`;\n  const hours = Math.floor(minutes / 60);\n  if (hours < 24)\n    return `${hours}h ${minutes % 60}m`;\n  const days = Math.floor(hours / 24);\n  return `${days}d ${hours % 24}h`;\n}\nfunction formatDatetime(ts) {\n  return new Date(ts).toISOString().replace(\"T\", `\n`).slice(0, 19);\n}\nfunction stageColorPalette(stageCount) {\n  const palettes = {\n    2: [\"#facc15\", \"#9ca3af\"],\n    3: [\"#facc15\", \"#a3e635\", \"#9ca3af\"],\n    4: [\"#facc15\", \"#60a5fa\", \"#a3e635\", \"#9ca3af\"]\n  };\n  const base = palettes[stageCount] ?? [\n    \"#facc15\",\n    \"#c084fc\",\n    \"#60a5fa\",\n    \"#a3e635\",\n    \"#9ca3af\"\n  ];\n  if (stageCount <= base.length)\n    return base.slice(0, stageCount);\n  return base.concat(new Array(stageCount - base.length).fill(base[base.length - 1]));\n}\nfunction repoColorPalette(count) {\n  const colors = [\n    \"#60a5fa\",\n    \"#f97316\",\n    \"#a3e635\",\n    \"#c084fc\",\n    \"#f43f5e\"\n  ];\n  if (count <= colors.length)\n    return colors.slice(0, count);\n  return colors.concat(new Array(count - colors.length).fill(colors[colors.length - 1]));\n}\nfunction stageKeyOf(d, multiRepo) {\n  return multiRepo ? makeStageKey(d.stage, d.artifactInfo.repositoryUrl) : d.stage;\n}\nfunction getOption(context) {\n  const rawDatums = dataFramesToDatums(context.panel.data.series);\n  if (rawDatums.length === 0) {\n    return {\n      title: {\n        text: \"No data\",\n        left: \"center\",\n        top: \"center\",\n        textStyle: { color: \"#aaaaaa\", fontSize: 16 }\n      }\n    };\n  }\n  const { series, domains } = transformData(rawDatums);\n  const stages = domains.stages;\n  const multiRepo = domains.multiRepo;\n  let colorMap;\n  if (multiRepo) {\n    const repos = [];\n    for (const key of stages) {\n      if (isSeparator(key))\n        repos.push(separatorRepo(key));\n    }\n    const repoColors = repoColorPalette(repos.length);\n    const repoColorMap = new Map(repos.map((r, i) => [r, repoColors[i]]));\n    colorMap = new Map(stages.map((key) => {\n      if (isSeparator(key))\n        return [key, \"#4b5563\"];\n      const { repo } = parseStageKey(key);\n      return [key, repoColorMap.get(repo ?? \"\") ?? \"#9ca3af\"];\n    }));\n  } else {\n    const colors = stageColorPalette(stages.length);\n    colorMap = new Map(stages.map((s, i) => [s, colors[i]]));\n  }\n  const timeRange = context.panel.data.timeRange ?? context.grafana?.timeRange;\n  const toNum = (v) => v === undefined ? undefined : typeof v === \"number\" ? v : v.valueOf();\n  const trFrom = toNum(timeRange?.from) ?? domains.timestampMin;\n  const trTo = toNum(timeRange?.to) ?? domains.timestampMax;\n  let domainMax = Math.max(domains.timestampMax, trTo);\n  let domainMin = Math.min(domains.timestampMin, trFrom);\n  const domainMargin = (domainMax - domainMin) * 0.05;\n  domainMax += domainMargin;\n  domainMin -= domainMargin;\n  const xMid = (domainMin + domainMax) / 2;\n  const MS_PER_WEEK = 7 * 24 * 60 * 60 * 1000;\n  const summaries = summarizeSortedStages(series, stages, multiRepo);\n  const tableRows = summaries.map((s) => {\n    const sum = s.getSummary();\n    if (isSeparator(sum.stage)) {\n      const repo = separatorRepo(sum.stage);\n      return {\n        isSeparator: true,\n        stage: repo || \"(no repository)\",\n        freq: \"\",\n        transition: \"\",\n        latest: \"\",\n        latestVersion: \"\"\n      };\n    }\n    const rawStage = multiRepo ? parseStageKey(sum.stage).stage : sum.stage;\n    const displayStage = stageDisplayLabel(rawStage);\n    let freq;\n    if (sum.countTimestamp <= 1 || sum.firstTimestamp === null || sum.lastTimestamp === null) {\n      freq = \"-\";\n    } else {\n      const windowMs = sum.lastTimestamp - sum.firstTimestamp;\n      const weeks = windowMs / MS_PER_WEEK;\n      freq = weeks > 0 ? `${((sum.countTimestamp - 1) / weeks).toFixed(2)}/w` : \"-\";\n    }\n    return {\n      isSeparator: false,\n      stage: displayStage,\n      freq,\n      transition: durationFormat(sum.intervalAverage),\n      latest: sum.lastTimestamp ? formatDatetime(sum.lastTimestamp) : \"N/A\",\n      latestVersion: sum.lastVersion ?? \"\"\n    };\n  });\n  const COL_X = [0, 115, 175, 265, 375];\n  const HEADERS = [\"Stage\", \"Freq\", \"Transition\", \"Latest\", \"Latest Version\"];\n  const option = {\n    backgroundColor: \"transparent\",\n    animation: false,\n    grid: {\n      left: \"2%\",\n      right: \"37%\",\n      bottom: \"15%\",\n      top: \"5%\",\n      containLabel: true\n    },\n    xAxis: {\n      type: \"time\",\n      min: domainMin,\n      max: domainMax,\n      axisLabel: {\n        formatter: (val) => {\n          const d = new Date(val);\n          const range = domainMax - domainMin;\n          if (range < 2 * 60 * 60 * 1000) {\n            return d.toISOString().slice(11, 19);\n          }\n          if (range < 2 * 24 * 60 * 60 * 1000) {\n            return d.toISOString().slice(11, 16);\n          }\n          return d.toISOString().slice(0, 10);\n        },\n        rotate: 25\n      },\n      splitLine: { show: false }\n    },\n    yAxis: {\n      type: \"category\",\n      data: stages,\n      axisLine: { show: false },\n      axisTick: { show: false },\n      axisLabel: { show: false },\n      splitLine: {\n        show: true,\n        lineStyle: { color: \"rgba(255,255,255,0.1)\" }\n      }\n    },\n    series: [\n      {\n        type: \"custom\",\n        name: \"timeline-lines\",\n        coordinateSystem: \"cartesian2d\",\n        clip: true,\n        silent: true,\n        tooltip: { show: false },\n        renderItem: (params, api) => {\n          const group = series[params.dataIndex];\n          if (!group || group.length < 2)\n            return { type: \"group\", children: [] };\n          const latest = group.reduce((a, b) => b.timestamp > a.timestamp ? b : a);\n          const lineColor = colorMap.get(stageKeyOf(latest, multiRepo)) ?? \"#9ca3af\";\n          const hasVersion = group[0]?.artifactInfo.hasVersion ?? true;\n          if (!hasVersion)\n            return { type: \"group\", children: [] };\n          const subGroups = new Map;\n          for (const d of group) {\n            const key = d.lineKey;\n            if (!subGroups.has(key))\n              subGroups.set(key, []);\n            subGroups.get(key)?.push(d);\n          }\n          const children = [];\n          for (const subGroup of subGroups.values()) {\n            if (subGroup.length < 2)\n              continue;\n            const points = subGroup.map((d) => api.coord([d.timestamp, stageKeyOf(d, multiRepo)]));\n            children.push({\n              type: \"polyline\",\n              shape: { points },\n              style: {\n                stroke: lineColor,\n                lineWidth: 1.5,\n                fill: \"none\",\n                opacity: 0.6\n              }\n            });\n          }\n          return { type: \"group\", children };\n        },\n        data: series.map((_, i) => i)\n      },\n      {\n        type: \"scatter\",\n        name: \"timeline-dots\",\n        coordinateSystem: \"cartesian2d\",\n        clip: true,\n        symbolSize: 12,\n        data: series.flatMap((group) => {\n          const latest = group.reduce((a, b) => b.timestamp > a.timestamp ? b : a);\n          const dotColor = colorMap.get(stageKeyOf(latest, multiRepo)) ?? \"#9ca3af\";\n          const hasVersion = group[0]?.artifactInfo.hasVersion ?? true;\n          return group.map((d) => ({\n            value: [d.timestamp, stageKeyOf(d, multiRepo)],\n            symbol: hasVersion ? \"circle\" : \"emptyCircle\",\n            itemStyle: { color: dotColor, borderColor: dotColor, opacity: 0.9 },\n            tooltipText: `<b>${d.artifactInfo.base}</b>` + `<br/>version: ${d.artifactInfo.version ?? \"(none)\"}` + (d.artifactInfo.tags.size > 0 ? `<br/>tags: ${Array.from(d.artifactInfo.tags).join(\", \")}` : \"\") + `<br/>action: ${d.action}` + (d.entity_type && d.entity_type !== \"artifact\" ? `<br/>entity: ${d.entity_id} (${d.entity_type})` : \"\") + (d.environment_id ? `<br/>environment: ${d.environment_id}` : \"\") + `<br/>stage: ${d.stage.replaceAll(`\n`, \" / \")}` + `<br/>timestamp: ${new Date(d.timestamp).toISOString()}`\n          }));\n        }),\n        tooltip: { show: true }\n      },\n      {\n        type: \"custom\",\n        name: \"table-header\",\n        coordinateSystem: \"none\",\n        silent: true,\n        tooltip: { show: false },\n        renderItem: (_params, api) => {\n          const canvasW = api.getWidth();\n          const canvasH = api.getHeight();\n          const tableX = canvasW * 0.645;\n          const topY = canvasH * 0.04;\n          return {\n            type: \"group\",\n            children: HEADERS.map((h, i) => ({\n              type: \"text\",\n              style: {\n                text: h,\n                x: tableX + COL_X[i],\n                y: topY,\n                fill: \"#888888\",\n                fontSize: 11,\n                fontWeight: \"bold\",\n                textAlign: \"left\"\n              }\n            }))\n          };\n        },\n        data: [0]\n      },\n      {\n        type: \"custom\",\n        name: \"table-rows\",\n        coordinateSystem: \"cartesian2d\",\n        clip: false,\n        silent: false,\n        tooltip: { show: true },\n        renderItem: (params, api) => {\n          const i = params.dataIndex;\n          const row = tableRows[i];\n          const stageKey = stages[i];\n          const [_x, cy] = api.coord([xMid, stageKey]);\n          const canvasW = api.getWidth();\n          const tableX = canvasW * 0.645;\n          if (row.isSeparator) {\n            return {\n              type: \"group\",\n              children: [\n                {\n                  type: \"text\",\n                  style: {\n                    text: `── ${row.stage}`,\n                    x: tableX,\n                    y: cy,\n                    fill: \"#6b7280\",\n                    fontSize: 10,\n                    fontStyle: \"italic\",\n                    textAlign: \"left\"\n                  }\n                }\n              ]\n            };\n          }\n          const stageColor = colorMap.get(stageKey) ?? \"#9ca3af\";\n          const cellValues = [\n            row.stage,\n            row.freq,\n            row.transition,\n            row.latest,\n            row.latestVersion\n          ];\n          const textColors = [\n            stageColor,\n            \"#cccccc\",\n            \"#cccccc\",\n            \"#aaaaaa\",\n            \"#60a5fa\"\n          ];\n          return {\n            type: \"group\",\n            children: cellValues.flatMap((text, ci) => {\n              const lines = (text ?? \"\").split(`\n`);\n              return lines.map((line, li) => ({\n                type: \"text\",\n                style: {\n                  text: line,\n                  x: tableX + COL_X[ci],\n                  y: cy - (lines.length - 1) * 6 + li * 12,\n                  fill: textColors[ci],\n                  fontSize: 11,\n                  textAlign: \"left\"\n                }\n              }));\n            })\n          };\n        },\n        data: stages.map((stageKey) => ({\n          value: [xMid, stageKey],\n          stageKey\n        })),\n        encode: { x: 0, y: 1 }\n      }\n    ],\n    tooltip: {\n      trigger: \"item\",\n      confine: true,\n      enterable: false,\n      formatter: (params) => {\n        if (params.seriesName === \"timeline-dots\") {\n          return params.data?.tooltipText ?? \"\";\n        }\n        if (params.seriesName === \"table-rows\") {\n          const raw = params.data?.stageKey ?? \"\";\n          if (!raw || raw.startsWith(REPO_SEPARATOR_PREFIX))\n            return \"\";\n          const parts = raw.split(`\n`);\n          if (parts.length === 3)\n            return `<b>${parts[0]}</b><br/>entity: ${parts[1]}<br/>env: ${parts[2]}`;\n          if (parts.length === 2)\n            return `<b>${parts[0]}</b><br/>${parts[1]}`;\n          return `<b>${raw}</b>`;\n        }\n        return \"\";\n      }\n    },\n    brush: {\n      xAxisIndex: 0,\n      brushType: \"lineX\",\n      brushMode: \"single\",\n      toolbox: []\n    },\n    toolbox: {\n      show: false\n    }\n  };\n  const instance = context.panel.chart;\n  if (instance && context.grafana?.locationService) {\n    const locationService = context.grafana.locationService;\n    instance.off(\"brushEnd\");\n    instance.on(\"brushEnd\", (params) => {\n      const p = params;\n      const coordRange = p.areas?.[0]?.coordRange;\n      if (!coordRange || coordRange[1] <= coordRange[0]) {\n        instance.dispatchAction({ type: \"brush\", areas: [] });\n        return;\n      }\n      const [from, to] = coordRange;\n      locationService.partial({ from: String(Math.round(from)), to: String(Math.round(to)) }, true);\n      instance.dispatchAction({ type: \"brush\", areas: [] });\n    });\n    setTimeout(() => {\n      instance.dispatchAction({\n        type: \"takeGlobalCursor\",\n        key: \"brush\",\n        brushOption: { brushType: \"lineX\", brushMode: \"single\" }\n      });\n    }, 0);\n  }\n  return option;\n}\nreturn getOption(context);\n",
        "renderer": "canvas",
        "editor": {
          "format": "javascript"
        }
      },
      "title": "$artifact_fnames",
      "repeat": "artifact_fnames",
      "maxPerRow": 1,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${datasource}"
      },
      "targets": [
        {
          "format": "table",
          "rawSql": "WITH artifact_filter AS (\n  SELECT\n    SPLIT_PART('${artifact_fnames:raw}', '\n', 1) AS base_name,\n    NULLIF(SPLIT_PART('${artifact_fnames:raw}', '\n', 2), '') AS repository_url\n),\nartifact_nodes AS (\n  SELECT n.node_id\n  FROM cdviz.graph_nodes n, artifact_filter\n  WHERE n.node_type = 'artifact'\n    AND (\n      n.node_id SIMILAR TO 'pkg:' || artifact_filter.base_name || '(@|\\\\?)%'\n      OR n.node_id = 'pkg:' || artifact_filter.base_name\n    )\n    AND (\n      artifact_filter.repository_url IS NULL\n      OR n.node_id SIMILAR TO '%repository_url=' || artifact_filter.repository_url || '(&%)?'\n    )\n),\n-- Run-type events raw: extract definition name for stage grouping.\n-- pipelinerun/taskrun/testcaserun/testsuiterun each have a \"definition\" field\n-- (pipelineName, taskName, testCase.name|id, testSuite.name|id) that is stable\n-- across multiple runs, unlike subject.id which is unique per run instance.\nrun_events_raw AS (\n  SELECT\n    cl.timestamp,\n    cl.subject || '.' || cl.predicate      AS action,\n    ge.relation                            AS relation,\n    CASE cl.subject\n      WHEN 'pipelinerun'  THEN COALESCE(cl.payload -> 'subject' -> 'content' ->> 'pipelineName', cl.payload -> 'subject' ->> 'id')\n      WHEN 'taskrun'      THEN COALESCE(cl.payload -> 'subject' -> 'content' ->> 'taskName', cl.payload -> 'subject' ->> 'id')\n      WHEN 'testcaserun'  THEN COALESCE(cl.payload -> 'subject' -> 'content' -> 'testCase' ->> 'name', cl.payload -> 'subject' -> 'content' -> 'testCase' ->> 'id', cl.payload -> 'subject' ->> 'id')\n      WHEN 'testsuiterun' THEN COALESCE(cl.payload -> 'subject' -> 'content' -> 'testSuite' ->> 'name', cl.payload -> 'subject' -> 'content' -> 'testSuite' ->> 'id', cl.payload -> 'subject' ->> 'id')\n    END                                    AS run_def_name,\n    an.node_id                             AS artifact_id,\n    gn.node_type                           AS entity_type,\n    cl.payload -> 'subject' ->> 'id'       AS entity_id,\n    cl.payload -> 'subject' -> 'content' -> 'environment' ->> 'id' AS environment_id\n  FROM artifact_nodes an\n  JOIN cdviz.graph_edges ge\n    ON ge.to_node_id = an.node_id\n    AND ge.relation != 'derivedFrom'\n  JOIN cdviz.graph_nodes gn ON gn.node_id = ge.from_node_id\n  JOIN cdviz.cdevents_lake cl\n    ON cl.subject = gn.node_type\n    AND cl.payload -> 'subject' ->> 'id' = ge.from_node_id\n    AND cl.subject IN ('pipelinerun', 'taskrun', 'testcaserun', 'testsuiterun')\n    AND (\n      cl.payload -> 'subject' -> 'content' ->> 'artifactId' = an.node_id\n      OR EXISTS (\n        SELECT 1\n        FROM jsonb_array_elements(\n          COALESCE(cl.payload -> 'customData' -> 'links', '[]'::jsonb)\n        ) AS lnk(v)\n        WHERE lnk.v -> 'subject' ->> 'id' = an.node_id\n      )\n    )\n  WHERE $__timeFilter(cl.timestamp)\n)\n-- Part 1: direct artifact events (published, signed, packaged, …)\nSELECT\n  cl.timestamp,\n  cl.predicate                             AS action,\n  cl.predicate                             AS stage,\n  cl.payload -> 'subject' ->> 'id'         AS artifact_id,\n  'artifact'::text                         AS entity_type,\n  cl.payload -> 'subject' ->> 'id'         AS entity_id,\n  NULL::text                               AS environment_id\nFROM artifact_nodes an\nJOIN cdviz.cdevents_lake cl\n  ON cl.subject = 'artifact'\n  AND cl.payload -> 'subject' ->> 'id' = an.node_id\nWHERE $__timeFilter(cl.timestamp)\n\nUNION ALL\n\n-- Part 2: non-run related-entity events (service deployments, …)\n-- Joined through graph_edges with an artifact-match constraint so each event\n-- appears exactly once — for the specific artifact version it references in its\n-- own payload (content.artifactId or customData.links), never fanning out to\n-- all versions the entity was historically linked to.\nSELECT\n  cl.timestamp,\n  cl.subject || '.' || cl.predicate        AS action,\n  CASE\n    WHEN cl.payload -> 'subject' -> 'content' -> 'environment' ->> 'id' IS NOT NULL\n      THEN ge.relation\n        || E'\\\n' || (cl.payload -> 'subject' ->> 'id')\n        || E'\\\n' || (cl.payload -> 'subject' -> 'content' -> 'environment' ->> 'id')\n    ELSE cl.predicate\n        || E'\\\n' || (cl.payload -> 'subject' ->> 'id')\n  END                                      AS stage,\n  an.node_id                               AS artifact_id,\n  gn.node_type                             AS entity_type,\n  cl.payload -> 'subject' ->> 'id'         AS entity_id,\n  cl.payload -> 'subject' -> 'content' -> 'environment' ->> 'id' AS environment_id\nFROM artifact_nodes an\nJOIN cdviz.graph_edges ge\n  ON ge.to_node_id = an.node_id\n  AND ge.relation != 'derivedFrom'\nJOIN cdviz.graph_nodes gn ON gn.node_id = ge.from_node_id\nJOIN cdviz.cdevents_lake cl\n  ON cl.subject = gn.node_type\n  AND cl.payload -> 'subject' ->> 'id' = ge.from_node_id\n  AND cl.subject NOT IN ('pipelinerun', 'taskrun', 'testcaserun', 'testsuiterun')\n  AND (\n    cl.payload -> 'subject' -> 'content' ->> 'artifactId' = an.node_id\n    OR EXISTS (\n      SELECT 1\n      FROM jsonb_array_elements(\n        COALESCE(cl.payload -> 'customData' -> 'links', '[]'::jsonb)\n      ) AS lnk(v)\n      WHERE lnk.v -> 'subject' ->> 'id' = an.node_id\n    )\n  )\nWHERE $__timeFilter(cl.timestamp)\n\nUNION ALL\n\n-- Part 3: run-type events (pipelinerun, taskrun, testcaserun, testsuiterun)\n-- Stage uses the definition name so all runs of the same pipeline/task/test share\n-- one Y-axis row. Only the last event per definition per artifact is shown.\nSELECT\n  timestamp,\n  action,\n  relation || E'\\\n' || run_def_name       AS stage,\n  artifact_id,\n  entity_type,\n  entity_id,\n  environment_id\nFROM (\n  SELECT\n    *,\n    ROW_NUMBER() OVER (\n      PARTITION BY artifact_id, entity_id\n      ORDER BY timestamp DESC\n    )                                      AS rn\n  FROM run_events_raw\n) ranked\nWHERE rn = 1",
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${datasource}"
          },
          "editorMode": "code",
          "rawQuery": true
        }
      ],
      "gridPos": {
        "h": 9,
        "w": 12,
        "x": 0,
        "y": 0
      }
    }
  ],
  "version": 2026040117
}
